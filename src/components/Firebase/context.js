import React from "react";


//instanciation de la class firebase afin de pouvoir utiliser les fonctions de firebase sur l'ensemble de l'application
const FireBaseContext = React.createContext(null);
//provider
//FireBaseContext.Provider

//consumer
//FireBaseContext.Consumer
//on export le context
export default FireBaseContext;